const domain = 'http://localhost:3300';

exports.url = {
    // User Authentication 
    ADD_REGISTER: `${domain}/register/add`,
    AUTHENTICATE_USR: `${domain}/login/add`,
    graphql_url: `${domain}/graphql`,
   
}